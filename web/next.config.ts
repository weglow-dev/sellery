import type { NextConfig } from "next";

/**
 * 슬라이스 1: 빈 설정 (docs/app-plan.md §1).
 * 도메인 이전(sellery.co.kr → sellery-app) 시 프로토타입 경로 리다이렉트를 여기(`redirects()`)에 둔다 (§13).
 */
const nextConfig: NextConfig = {};

export default nextConfig;
